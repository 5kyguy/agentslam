[project]
name = "chain-slam-agents"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["websockets>=13.0"]

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.backends._legacy:_Backend"
